# Claude context for bioparsers

Read the `docs/project_context.md` file for important context. 

There is a local conda environment `./env/` that should be used to run package scripts and tests. Activate this before running python/pip commands. Do not install into it or another environment without permission.
