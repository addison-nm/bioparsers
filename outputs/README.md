# Outputs

This directory is intended to hold the datasets constructed from the `bioparsers` Builder framework.
After parsing the necessary databases, we can then use the generated artifacts to compose structured JSONL datasets, with defined schemas. These structured datasets can then be used in downstream tasks where a consistent JSONL input is expected.
